# إدارة المستخدمين (Users Management)

## معلومات الصفحة

- **المسار**: `/management/users`
- **القائمة**: Management > Users
- **الصلاحيات المطلوبة**: `view-users` (للعرض)، `create-users` (للإضافة)، `edit-users` (للتعديل)، `delete-users` (للحذف).

---

## 👥 نظرة عامة

قسم إدارة المستخدمين يتيح للمسؤولين التحكم الكامل في حسابات الأشخاص الذين يحق لهم الدخول للنظام.

### بطاقات الإحصائيات (Stats Cards)
في أعلى الصفحة، ستجد مؤشرات سريعة:
1.  **Total Users**: العدد الكلي للمستخدمين المسجلين.
2.  **Active Users**: عدد المستخدمين النشطين حالياً.

---

## 📋 قائمة المستخدمين

يعرض الجدول البيانات التالية لكل مستخدم:

| العمود | الوصف |
|--------|-------|
| **Avatar** | الصورة الشخصية للمستخدم (أو الأحرف الأولى من اسمه). |
| **Name** | الاسم الكامل. |
| **Email** | البريد الإلكتروني (يستخدم لتسجيل الدخول). |
| **Roles** | الأدوار والصلاحيات الممنوحة له (مثل `Admin`, `Manager`). |
| **Created At** | تاريخ إنشاء الحساب. |
| **Actions** | أزرار التحكم (تعديل ✏️ / حذف 🗑️). |

---

## ➕ إضافة مستخدم جديد

لإضافة مستخدم، اضغط زر **Add User** الأزرق. ستظهر استمارة تتطلب:

1.  **Name**: الاسم (إجباري).
2.  **Email**: البريد الإلكتروني (يجب أن يكون فريداً).
3.  **Password**: كلمة المرور (يجب تأكيدها).
4.  **Avatar**: اختياري (صورة بصيغة JPG/PNG بحد أقصى 2MB).
5.  **Roles**: تحديد دور واحد أو أكثر للمستخدم (مثلاً أعطه دور `Employee` ليتمكن من رؤية المهام فقط).

> [!IMPORTANT]
> **تنبيه:**
> التأكد من منح المستخدم الدور الصحيح يضمن أمان النظام وعدم وصوله لبيانات حساسة.

---

## ✏️ تعديل وحذف

- **التعديل**: يمكنك تغيير بيانات المستخدم (الاسم، الأدوار) في أي وقت. لتغيير كلمة المرور، اترك الحقل فارغاً إذا كنت لا تود تغييرها.
- **الحذف**: عند ضغط زر الحذف، سيطلب النظام تأكيداً إضافياً لمنع الحذف عن طريق الخطأ.

---

**التالي**: [إدارة الأدوار والصلاحيات →](02-roles-permissions.md)
