# إدارة الأدوار والصلاحيات (Roles & Permissions)

## معلومات الصفحة

- **المسار**: `/management/roles` و `/management/permissions`
- **القائمة**: Management > Roles / Permissions
- **الصلاحيات المطلوبة**: `view-roles` / `view-permissions`

---

## 🛡️ ما الفرق بين الدور والصلاحية؟

- **الدور (Role)**: هو مسمى وظيفي (مثل *Manager*, *Employee*, *Admin*). يتم تعيين الدور للمستخدم.
- **الصلاحية (Permission)**: هي إذن للقيام بفعل محدد (مثل *Create Project*, *Delete Task*).

**العلاقة**: أنت تمنح الصلاحيات للدور، ثم تمنح الدور للمستخدم.
> User (Ahmed) -> Role (Manager) -> Permissions (Create Project, Edit Task).

---

## 🔑 إدارة الأدوار (Roles)

تعرض صفحة الـ **Roles** قائمة بجميع المجموعات المعرفة في النظام.

### الجدول
| العمود | الوصف |
|--------|-------|
| **Role Name** | اسم الدور (ملون). الدور `Super Admin` مميز باللون الأحمر وله كافة الصلاحيات. |
| **Permissions Count** | عدد الصلاحيات الممنوحة لهذا الدور. |
| **Users Count** | كم عدد المستخدمين الذين يحملون هذا الدور. |

### إضافة/تعديل دور
عند إنشاء دور جديد، ستظهر لك قائمة بجميع صلاحيات النظام (Checkboxes). قم بتحديد ما يستطيع صاحب هذا الدور فعله بدقة.

> [!WARNING]
> **Super Admin**
> دور "Super Admin" محمي ولا يمكن حذفه أو تعديل صلاحياته، لأنه يملك حق الوصول لكل شيء تلقائياً.

---

## 📜 سجل الصلاحيات (Permissions List)

صفحة **Permissions** هي مرجع لك لمعرفة كل "إذن" في النظام ومن يملكه.
يتم عرض الصلاحيات بشكل منظم داخل **قوائم قابلة للطي (Accordion)** حسب القسم (Module):

- **User Management**: `view-users`, `create-users`, ...
- **Project Management**: `view-projects`, `create-projects`, ...
- **Task Management**: `view-tasks`, `delete-tasks`, ...

لكل صلاحية، سترى:
1.  **الاسم البرمجي**: (مثلاً `create-projects`).
2.  **الأدوار المرتبطة**: شارات توضح أي أدوار تملك حالياً هذه الصلاحية.

---

**نهاية قسم Management.**
[العودة للفهرس الرئيسي ←](../../index.md)
