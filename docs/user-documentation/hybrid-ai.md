# توثيق نظام الذكاء الاصطناعي الهجين 🧠

## نظرة عامة
يقدم نظام الذكاء الاصطناعي الهجين بنية "العقل المزدوج" (Dual Mind) لإدارة المشاريع. يجمع هذا النظام بين سرعة وخصوصية المنطق المحلي القائم على القواعد، والقدرات التحليلية المتقدمة للذكاء الاصطناعي التوليدي السحابي (مثل OpenAI و Gemini).

## البنية البرمجية (Architecture) 🛠️

### نمط الاستراتيجية (Strategy Pattern)
تمت إعادة هيكلة المنطق الأساسي باستخدام **نمط الاستراتيجية**:
- **DecisionStrategyInterface**: واجهة برمجية تحدد عقد التحليل.
- **LegacyDecisionStrategy (العقل المحلي)**: يستخدم منطقاً مبرمجاً مسبقاً (If/Else) لاكتشاف المهام المتأخرة وغيرها. سريع، مجاني، ويعمل بدون إنترنت.
- **GenerativeDecisionStrategy (العقل السحابي)**: يرسل سياقاً مختصراً إلى مزود ذكاء اصطناعي خارجي عبر `AIGateway`. يقدم توصيات دقيقة تراعي السياق.

### شبكة الأمان (Automatic Fallback) 🛡️
يتضمن النظام شبكة أمان قوية. إذا فشل الذكاء الاصطناعي السحابي (بسبب خطأ في API، انقطاع اتصال، أو تجاوز الحدود)، يقوم النظام **تلقائياً بالعودة** إلى العقل المحلي. هذا يضمن عدم تفويت التنبيهات الحرجة (مثل المهام المتأخرة).

### السياق المختصر (Compact Context)
لتحسين التكلفة، نستخدم دالة `buildCompactContext()` التي تقوم بفلترة بيانات المهمة لإرسال "الإشارات" الضرورية فقط للذكاء الاصطناعي، مما يقلل استهلاك التوكنز بنسبة تصل إلى 70%.

---

## دليل المستخدم للمدراء 📘

### أوضاع التشغيل

| الوضع | إعداد المزود | الأفضل لـ | المميزات | العيوب |
|------|------------------|----------|------|------|
| **العقل المحلي** | `Local (Fallback)` | المراقبة الروتينية، متطلبات الخصوصية العالية | فوري، مجاني، يعمل أوفلاين | منطق جامد، قدرات استنتاج محدودة |
| **العقل السحابي** | `OpenAI`, `Gemini`, `Claude` | حل المشكلات المعقدة، النصائح الإدارية | استنتاج عميق، حلول إبداعية | يستهلك تكلفة (Tokens)، أبطأ قليلاً |

### كيفية التبديل
1. اذهب إلى **لوحة تحكم الذكاء الاصطناعي (AI Control Panel)**.
2. ابحث عن قائمة **المزود (Provider)** في أعلى اليمين.
3. اختر "العقل" الذي تفضله:
   - اختر **Local** لإجبار النظام على استخدام القواعد المحلية.
   - اختر **OpenAI/Gemini** لتفعيل الذكاء الاصطناعي التوليدي.
4. التغيير يتم فوراً.

### متى تستخدم كل وضع؟
- **استخدم الوضع المحلي** عندما تريد فقط تتبع المواعيد النهائية والعوائق البسيطة دون تكلفة إضافية.
- **استخدم الوضع السحابي** عندما تحتاج إلى أن يشرح لك الذكاء الاصطناعي *لماذا* يتعثر المشروع أو يقترح *كيفية* حل سلسلة من الاعتمادات المعقدة.
